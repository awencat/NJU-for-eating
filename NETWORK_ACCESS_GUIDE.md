# 🔌 局域网访问配置指南

> 解决其他电脑无法连接后端服务的问题

---

## 🎯 问题现象

**症状**: 
- ✅ 你自己电脑可以正常访问 `http://127.0.0.1:5000`
- ❌ 朋友电脑访问 `http://你的IP:5000` 连接失败/超时

**常见错误**:
```
ERR_CONNECTION_REFUSED
ERR_CONNECTION_TIMED_OUT
无法连接到服务器
```

---

## 🔍 快速诊断

### Step 1: 运行诊断工具

在你的电脑上运行:

```bash
cd backend
python network_diagnostic.py
```

**输出示例**:
```
======================================================================
🔍 网络连接诊断工具
======================================================================

📍 本机局域网IP: 192.168.1.100
   其他设备应访问: http://192.168.1.100:5000

🔌 检查端口 5000...
   ✅ 端口 5000 正在监听

🛡️  防火墙状态:
   ⚠️  防火墙已开启
   💡 可能需要添加入站规则允许5000端口

🧪 测试本地连接...
   ✅ localhost:5000 - 连接成功

🧪 测试局域网连接...
   ✅ 192.168.1.100:5000 - 连接成功
```

**关键信息**:
- **本机局域网IP**: 朋友需要访问的地址
- **端口状态**: 确认Flask正在监听
- **防火墙**: 是否需要配置规则

---

## ✅ 解决方案(按优先级)

### 方案1: 确认Flask绑定到0.0.0.0 (最重要!)

**检查config.py:**

```python
# backend/config.py
HOST = os.getenv('HOST', '0.0.0.0')  # ✅ 必须是 0.0.0.0
PORT = int(os.getenv('PORT', 5000))
```

**❌ 错误配置:**
```python
HOST = '127.0.0.1'  # 只能本机访问
HOST = 'localhost'  # 只能本机访问
```

**✅ 正确配置:**
```python
HOST = '0.0.0.0'    # 允许所有网络接口访问
```

**验证方法:**
启动服务后应该看到:
```
服务地址: http://0.0.0.0:5000
```

如果显示 `http://127.0.0.1:5000`,说明配置错误!

---

### 方案2: Windows防火墙配置

#### 方法A: 临时关闭防火墙(测试用)

1. 打开"控制面板" → "Windows Defender 防火墙"
2. 点击左侧"启用或关闭 Windows Defender 防火墙"
3. 选择"关闭 Windows Defender 防火墙"(专用网络和公用网络)
4. 点击确定
5. 让朋友重新测试

⚠️ **注意**: 测试完成后记得重新开启防火墙!

#### 方法B: 添加入站规则(推荐)

1. **打开高级防火墙设置**:
   - 按 `Win + R`
   - 输入 `wf.msc`
   - 回车

2. **创建新规则**:
   - 左侧点击"入站规则"
   - 右侧点击"新建规则..."

3. **配置规则**:
   ```
   规则类型: 端口
   协议和端口: TCP, 特定本地端口: 5000
   操作: 允许连接
   配置文件: 域、专用、公用(全选)
   名称: Flask Backend Port 5000
   ```

4. **完成并测试**

---

### 方案3: 获取正确的访问地址

**在你电脑上查询IP:**

```bash
# Windows PowerShell
ipconfig | findstr "IPv4"

# 或者运行诊断工具
cd backend
python network_diagnostic.py
```

**输出示例:**
```
IPv4 地址 . . . . . . . . . . . . : 192.168.1.100
```

**朋友电脑的访问地址:**
```
✅ 正确: http://192.168.1.100:5000/api/health
❌ 错误: http://127.0.0.1:5000 (这是朋友自己的电脑)
❌ 错误: http://localhost:5000 (这是朋友自己的电脑)
```

**测试连通性:**
在朋友电脑的浏览器访问:
```
http://192.168.1.100:5000/api/health
```

应该返回:
```json
{
  "code": 200,
  "status": "success",
  "message": "服务运行正常"
}
```

---

### 方案4: 修改前端API地址

如果朋友要使用前端页面,需要修改前端配置:

**编辑文件:** `frontend/js/api.js`

```javascript
// 原来的配置(只能本机访问)
const BASE_URL = 'http://127.0.0.1:5000/api';

// 修改为你的局域网IP
const BASE_URL = 'http://192.168.1.100:5000/api';
```

**或者使用环境变量:**

```javascript
// 从URL参数读取后端地址
const urlParams = new URLSearchParams(window.location.search);
const backendHost = urlParams.get('backend') || '127.0.0.1';
const BASE_URL = `http://${backendHost}:5000/api`;
```

然后访问:
```
http://192.168.1.100:5500/?backend=192.168.1.100
```

---

### 方案5: 检查网络环境

#### 确认在同一局域网

**检查方法:**
1. 你电脑的IP: `192.168.1.100`
2. 朋友电脑的IP: `192.168.1.xxx`
3. 前三段应该相同(`192.168.1`)

**如果不同:**
- 确保连接同一个WiFi
- 检查路由器设置

#### 检查路由器AP隔离

**症状**: 两台电脑都能上网,但互相ping不通

**解决方法:**
1. 登录路由器管理界面(通常是 `192.168.1.1`)
2. 找到"无线设置"或"高级设置"
3. 关闭"AP隔离"或"客户端隔离"
4. 保存并重启路由器

#### 测试网络连通性

**在朋友电脑上:**
```bash
# Windows CMD
ping 192.168.1.100

# 应该看到回复
Reply from 192.168.1.100: bytes=32 time<1ms TTL=128
```

**如果ping不通:**
- 检查防火墙是否阻止ICMP
- 检查路由器设置
- 尝试暂时关闭双方防火墙测试

---

## 🛠️ 常见问题排查

### Q1: 显示 "ERR_CONNECTION_REFUSED"

**原因**: 端口未监听或被拒绝

**解决:**
1. 确认Flask正在运行
2. 检查是否绑定到 `0.0.0.0`
3. 查看控制台是否有报错

```bash
# 检查端口占用
netstat -ano | findstr :5000

# 应该看到
TCP    0.0.0.0:5000    0.0.0.0:0    LISTENING    12345
```

---

### Q2: 显示 "ERR_CONNECTION_TIMED_OUT"

**原因**: 防火墙阻止或网络不通

**解决:**
1. 检查Windows防火墙(见方案2)
2. 检查杀毒软件防火墙
3. 测试ping连通性

```bash
# 在朋友电脑上测试
ping 192.168.1.100
telnet 192.168.1.100 5000
```

---

### Q3: 能ping通但无法访问网页

**原因**: 防火墙只允许ICMP,阻止TCP

**解决:**
1. 添加TCP 5000端口入站规则(见方案2方法B)
2. 或临时关闭防火墙测试

---

### Q4: 朋友的IP和你不在同一网段

**原因**: 连接了不同的网络

**解决:**
1. 确保都连接同一个WiFi
2. 如果使用手机热点,确保都连接该热点
3. 检查路由器DHCP分配是否正常

---

### Q5: 使用虚拟机或WSL

**特殊情况**: WSL2有独立的网络命名空间

**解决:**
```bash
# 在WSL2中查看IP
ip addr show eth0 | grep "inet "

# 或使用host.docker.internal
http://host.docker.internal:5000
```

---

## 📋 完整检查清单

在让朋友测试前,请确认:

- [ ] Flask服务正在运行 (`python app.py`)
- [ ] config.py中 `HOST = '0.0.0.0'`
- [ ] 控制台显示 `服务地址: http://0.0.0.0:5000`
- [ ] 已获取本机局域网IP (如 `192.168.1.100`)
- [ ] 防火墙已配置允许5000端口
- [ ] 朋友和你连接同一WiFi
- [ ] 路由器AP隔离已关闭
- [ ] 朋友能ping通你的IP
- [ ] 你在自己电脑能通过局域网IP访问

**快速验证命令:**
```bash
# 在你电脑上测试
curl http://192.168.1.100:5000/api/health

# 或在浏览器访问
http://192.168.1.100:5000/api/health
```

---

## 🚀 一键启动脚本(可选)

创建 `start_server.bat`:

```batch
@echo off
echo ========================================
echo  启动智慧校园餐厅推荐系统
echo ========================================

cd /d %~dp0backend

echo.
echo [1/3] 检查Python环境...
python --version

echo.
echo [2/3] 安装依赖...
pip install -r requirements.txt -q

echo.
echo [3/3] 启动服务...
echo.
python app.py

pause
```

双击即可启动,自动显示访问地址。

---

## 💡 最佳实践

### 开发环境

**你的电脑:**
```
后端: http://127.0.0.1:5000 (本地开发)
前端: http://127.0.0.1:5500 (Live Server)
```

**团队协作:**
```
后端: http://192.168.1.100:5000 (局域网共享)
前端: 修改api.js中的BASE_URL
```

### 生产环境

**部署到服务器:**
```python
# 使用Gunicorn
gunicorn -w 4 -b 0.0.0.0:8000 app:app

# 配合Nginx反向代理
server {
    listen 80;
    server_name your-domain.com;
    
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
    }
}
```

---

## 📞 仍然无法解决?

**提供以下信息寻求帮助:**

1. **你的电脑IP**: 运行 `ipconfig`
2. **朋友电脑IP**: 让朋友运行 `ipconfig`
3. **防火墙状态**: 运行 `network_diagnostic.py`
4. **错误截图**: 浏览器显示的错误信息
5. **测试结果**: 
   ```bash
   # 在你电脑上
   curl http://你的IP:5000/api/health
   
   # 在朋友电脑上
   ping 你的IP
   telnet 你的IP 5000
   ```

---

**最后更新**: 2026年4月23日  
**相关文档**: [README.md](../README.md) | [DEVELOPER_GUIDE.md](../DEVELOPER_GUIDE.md)
